{templateType Stepapo\Dataset\UI\ItemList\ItemListTemplate}

{snippet}
	<div n:class="row, $control->getDataset()->getItemListClass()" n:foreach="($items|batch:4) as $row" n:inner-foreach="$row as $item">
		{control 'item-' . $item->getValue($idColumnName)}
	</div>
	{if $control->getDataset()->getCurrentCount() == 0}
		<p>
			{_$text->noResults}
		</p>
	{/}
{/}
