{templateType Stepapo\Dataset\Control\FilterList\FilterListTemplate}

<div n:snippet>
	{foreach $columns as $column}
		{if $column->filter && !$column->filter->hide}
			{control 'filter-' . $column->name}
		{/}
	{/}
</div>
